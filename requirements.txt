--extra-index-url https://download.pytorch.org/whl/cu128
torch
torchvision
torchaudio
accelerate
transformers
diffusers
gradio
safetensors
sentencepiece
Pillow
numpy
pandas
matplotlib
tqdm
packaging
dadaptation==3.2
prodigyopt
wandb
lycoris_lora
schedulefree
pytorch-optimizer
bitsandbytes>=0.43.0

# tune_lora_scales.py — optional perceptual metrics
lpips        # --metric lpips               (LPIPS VGG perceptual loss)
piq          # --metric dists               (DISTS structure+texture similarity)
scipy        # --preview_schedule beta      (Beta timestep schedule)
# --metric gram / fft / gram_fft: torchvision (already above) + torch.fft (built-in)
